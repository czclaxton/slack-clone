// import { assert } from "chai";
const assert = require("chai").assert;
// const createTokens = require("../auth").createTokens;
// import { createTokens, refreshTokens, tryLogin } from "../auth";

describe("auth functions", function () {
  it("should create tokens", function () {
    // const [token, refreshToken] = createTokens(
    //   { id: "6", username: "test" },
    //   process.env.SECRET,
    //   process.env.SECRET2
    // );
    // console.log("TOKEN", token);
    // console.log("REFRESHTOKEN", refreshToken);

    assert.equal(2, 2);
  });
});

module.exports = {};
